DATABASE_URL=postgresql+psycopg://postgres:change-me@localhost:5432/warehouse_db
JWT_SECRET_KEY=replace-with-a-long-random-secret
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:8081,http://127.0.0.1:8081
AUTO_CREATE_SCHEMA=false
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_FROM=pantryos@example.com
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_SSL=false
# Set true only for local development; the API will return the OTP in its response.
DEV_RETURN_OTP=false